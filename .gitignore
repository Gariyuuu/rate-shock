.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store

# Cached raw downloads: reproducible via scripts/run_analysis.py.
# Kept out of git because they are third-party payloads (Fed/BLS HTML,
# FRBSF xlsx, Yahoo price CSV) that re-fetch cleanly from source.
data/raw/

# Large derived panels (regenerated by scripts/run_analysis.py in seconds)
data/processed/

# Vercel CLI metadata (local link state)
.vercel/
